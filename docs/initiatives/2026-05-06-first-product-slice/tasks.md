# 任务

- [x] 第 0 片：工程设计就绪门禁
- [ ] 门禁：`$writing-plans` 产出对齐后的实现计划
- [ ] 门禁：`$plan-eng-review` 批准对齐后的实现计划
- [ ] 门禁：`$executing-plans` 仅在前两个门禁通过后启动

新 session 的 `$goal` 覆盖整个第一产品切片长任务。第 1 片及之后由设计加固的 `implementation-plan-reconciliation.md` 产出前不得列入执行任务；产出后必须先通过 `$writing-plans` 和 `$plan-eng-review`，再进入 `$executing-plans`。
