# SegmentAttachment 转录面板任务

## Milestones

- [ ] 创建独立 spec，确认 `docs/specs/*` 为空。
- [ ] TDD 扩展 main / contract / preload / renderer API，让 finalized SegmentAttachment content response 返回 transcript。
- [ ] TDD 实现 renderer attachment panel 的 transcript 区。
- [ ] 更新 `docs/current/*` 当前事实。
- [ ] 完成运行时视觉验证、subagent 审查和 `npm run verify:quick`。
- [ ] 归档对应 spec，并在完成后归档本 initiative。
