# 验证

## 红绿重构证据

- RED 1：新增 `test/main/memoryFiles.test.ts` 中 `finalizes a draft into a durable memory directory` 后运行 `npm run test:main`，失败为 TypeScript TS2353：`workspaceId` 不是旧 `finalizeRecordingDraft` 入参，证明当前实现尚不支持 memory finalize contract。
- GREEN 1：新增 `src/main/memoryFiles.ts`，将 draft 改到 `.reo/drafts/recordings/<recordingId>/`，finalize 写入 `memories/<memoryId>/recordings/<recordingId>/`，并更新 workspace index 为 `memories[]`。更新旧 main tests 到新文件真源合同后，`npm run test:main` PASS。
- Coverage expansion：补充 title update、finalize rollback、append rollback、symlink guard、atomic fsync order、interrupted finalize recovery、missing recording repair、valid finalized ownership/audio byte rebuild、finalize staging fsync order 等行为测试。补充测试后 `npm run test:main` PASS。
- Review RED：Codex/subagent 第一轮审查后补充 6 条失败行为测试：duplicate durable recording 不得被 rollback 删除、stale draft recovery、同 memory 并发 append、existing memory append 返回单条 bytes、markdown save 刷新 index、同名 memory create 不得替换既有 memory。运行 `npm run test:main`，新增测试按预期失败。
- Review GREEN：修复 marker-owned rollback、draft cleanup order、per-memory write lock、single-recording summary、compat `recordings[]` projection、markdown save index refresh 和 existing memory preflight 后，`npm run test:main` PASS，56 个 main tests 全部通过。
- Review RED 2：Codex 第二轮指出 draft cleanup 失败后不能清 marker/宣称成功，以及 title update file truth 写入后 projection refresh 失败不能误报 `previous-file-preserved`。补充 cleanup failure、recovery marker retention、title update projection failure tests 后，`npm run test:main` 失败 3 项。
- Review GREEN 2：finalize 成功路径不再吞掉 draft cleanup failure；recovery 在 stale draft cleanup 失败时保留 marker；title update 以 `memory.json` 为真源成功返回，index refresh best-effort。`npm run test:main` PASS，59 个 main tests 全部通过。
- Review RED 3：Codex/subagent 后续审查指出 recovery 可能跟随 symlinked `recordings/`、finalized metadata schema 过宽、IPC duration 默认 0、detail-read 与 rebuild schema 不一致、renderer finalize 后当前 session `memories[]` stale、directory fsync unsupported 会误报失败。逐项补充对应失败测试。
- Review GREEN 3：收紧 recovery containment、finalized metadata schema、IPC finalize contract、renderer duration 传递、session merge helper 和 directory fsync fallback。`npm run verify:quick` PASS，main 74、renderer 33。
- Review RED 4：subagent 发现 `.reo` symlink IPC 初始化会写出 workspace、AGENTS conflict IPC 路径会留下 `.reo/workspace.lock`、finalized + stale draft 并存时 append 仍写 stale draft。新增 `initializeWorkspace rejects symlinked .reo before writing workspace files`、`initializeWorkspace returns AGENTS conflict without leaving a workspace lock`、`recording append rejects stale draft when a finalized recording already exists`，targeted tests 按预期失败。
- Review GREEN 4：新增 `.reo` metadata directory no-write preflight，IPC initialize 在 lock 前检查 target，lock/draft 初始化拒绝 symlinked `.reo`，audio append 写 draft 前先识别 finalized file truth。`npm run verify:quick` PASS，main 77、renderer 33。
- Review RED 5：Codex/subagent 复审发现 nested `.reo/drafts` symlink 仍可写出 workspace、open 非 Reo 目录会留下 lock、unsafe `memories/` target 会抛异常或留下 partial `.reo`。新增 `openWorkspace rejects non-Reo directories before writing a workspace lock`、`initializeWorkspace rejects unsafe memories targets before writing workspace files`、`initializeWorkspace rejects symlinked .reo draft ancestors before writing outside workspace`，targeted tests 按预期失败。
- Review GREEN 5：`.reo/drafts/recordings` 和 `memories/` 改为逐段 lstat/创建；open 在 lock 前确认 metadata 合法。`npm run verify:quick` PASS，main 80、renderer 33。
- Review RED 6：Codex 复审指出 open 现有 workspace 时 unsafe child directory 仍可能在 lock 后失败，以及 renderer durable duration 使用显示秒表会对 sub-second recording 写 0。新增 `openWorkspace rejects unsafe child directories before writing a workspace lock` 和 `uses sub-second recording duration instead of the rounded display timer`，targeted tests 按预期失败。
- Review GREEN 6：open preflight 补齐 `.reo/drafts/recordings` 和 `memories/` 安全检查；renderer 使用 active recording clock 计算 duration。`npm run verify:quick` PASS，main 81、renderer 34。
- Review RED 7：Codex 复审指出 finalized copy 的递归 `mkdir` 可能在 `memories/<memoryId>` 被替换成 symlink 后跟随到 workspace 外。新增 `preserves a draft when the memory parent is swapped to a symlink before staging`，`npx tsx --test test/main/memoryFiles.test.ts` 按预期失败，失败点为 workspace 外 `recordings/` 被创建。
- Review GREEN 7：finalized parent directory 改为逐段非递归创建、`lstat` 复核，并在 staging 前再次复核 `recordings/` parent。`npx tsx --test test/main/memoryFiles.test.ts` PASS，28 个 `memoryFiles` tests 全部通过；`npm run verify:quick` PASS，main 82、renderer 34。
- Review RED 8：Subagent 复审指出 IPC initialize/open 获取 lock 后抛异常会泄漏 lock；Codex 复审指出 transaction directory fsync 不支持时会误回滚 finalize。新增 `initializeWorkspace releases the lock when post-lock file writes throw`、`openWorkspace releases the lock when post-lock recovery or index writes throw`、`finalize transaction directory fsync tolerates unsupported directory fsync`，targeted tests 按预期失败。
- Review GREEN 8：IPC 获取 lock 后的文件阶段 catch 异常并释放 lock；transaction directory fsync 复用 unsupported directory fsync best-effort 判定。`npx tsx --test test/main/memoryFiles.test.ts` PASS，29 个 `memoryFiles` tests 全部通过；`npx tsx --test test/main/workspaceIpc.test.ts` PASS，10 个 IPC tests 全部通过；`npm run verify:quick` PASS，main 85、renderer 34。
- Review RED 9：Subagent 复审指出 IPC handle registration 抛异常时 lock 仍可能泄漏；Codex 复审指出 pre-expose finalize setup 会留下 `.reo-finalizing-*` 和空新 memory 目录。新增 `initializeWorkspace releases the lock when handle registration throws`、`openWorkspace releases the lock when handle registration throws`、`new memory finalize can retry with the same memory id after marker write failure`。`npx tsx --test test/main/workspaceIpc.test.ts` 按预期失败 2 项，错误为 `handle boom` 直接抛出；`npx tsx --test test/main/memoryFiles.test.ts` 按预期失败 1 项，错误为 expected rejection missing。
- Review GREEN 9：IPC register/response parse 失败路径统一释放 lock，已注册 handle 先 close；pre-expose finalize setup 失败清理 staging tree 和未提交的新 memory 目录。`npx tsx --test test/main/workspaceIpc.test.ts` PASS，12 个 IPC tests 全部通过；`npx tsx --test test/main/memoryFiles.test.ts` PASS，30 个 `memoryFiles` tests 全部通过。
- Review RED 10：Subagent 复审指出 pre-expose cleanup 在 memory 父目录 symlink swap 后可能删除 workspace 外同名 staging。新增 `pre-expose cleanup does not delete outside staging when memory parent is swapped to a symlink`，`npm run test:main` 按预期失败；失败点为外部 sentinel 被删除。
- Review GREEN 10：cleanup 改为使用 canonical workspace root，并逐段 `lstat` workspace 内目录；只清理已验证的 workspace 内 staging 和未提交 memory。`npm run test:main` PASS，89 个 main tests 全部通过。
- Review RED 11：Codex 复审指出 finalized audio symlink 可通过 manifest/chunk read 暴露 workspace 外字节，且 finalize 会清空 draft 中已保存 markdown。新增 `finalized audio symlinks are rejected before manifest or chunk reads` 和 `recording finalize preserves draft transcript and reflections markdown`。`npm run test:main` 按预期失败 2 项。
- Review GREEN 11：finalized lookup、index summary 和 audio read 改为 `lstat` audio 并拒绝 symlink/非文件；finalize 只补齐缺失 markdown，保留已有普通 transcript/reflections。`npm run test:main` PASS，91 个 main tests 全部通过。
- Review RED 12：Subagent 复审指出 draft `audio.webm` 被替换成目录时，finalize 会写入 invalid durable memory/index、删除 draft，却返回 `draft-preserved`。新增 `recording finalize rejects non-file draft audio before deleting the draft`，`npm run test:main` 按预期失败 1 项。
- Review GREEN 12：finalize 写 finalized metadata 前要求 staging audio 是普通文件；目录、symlink 或非文件都在 draft cleanup 前 rollback。`npm run test:main` PASS，92 个 main tests 全部通过。
- Review RED 13：Codex 复审指出 draft `audio.webm` 被替换成 symlink 后，append chunk 会写到 workspace 外并推进 metadata。新增 `recording draft rejects symlinked draft audio before appending chunks`，targeted test 按预期失败，失败点为 append 返回 ok。
- Review GREEN 13：append chunk 前复核 draft audio 为普通文件，并用 no-follow append 打开；symlink 或非文件返回 unsafe path，保留 draft metadata。`npx tsx --test test/main/recordingDrafts.test.ts --test-name-pattern 'recording draft rejects symlinked draft audio before appending chunks'` PASS；`npm run test:main` PASS，93 个 main tests 全部通过。
- Review RED 14：Codex 复审指出 audio chunk read 在 validation 后重新按 path 读取存在 TOCTOU。新增 `audio chunk reads use the guarded file handle after audio validation`，targeted test 按预期失败，失败点为读到了被替换 symlink 指向的 workspace 外字节。
- Review GREEN 14：audio read 改为 no-follow open、fstat、同 handle read；validation 后路径被替换也只读取已验证文件。`npx tsx --test test/main/recordingReads.test.ts --test-name-pattern 'audio chunk reads use the guarded file handle after audio validation'` PASS；`npm run test:main` PASS，94 个 main tests 全部通过。
- Review RED 15：Subagent 复审指出 dangling `AGENTS.md` symlink 会绕过 initialize no-write preflight，并在后续写入失败时留下 `.reo/` 和 `memories/`。新增 `dangling AGENTS.md symlink conflict does not write workspace files` 和 `initializeWorkspace treats dangling AGENTS.md symlink as conflict before lock`，targeted tests 按预期失败 2 项。
- Review GREEN 15：`AGENTS.md` conflict preflight 改用 `lstat`，dangling symlink 和任何 symlink 条目都按已有 `AGENTS.md` conflict 处理。`npx tsx --test test/main/workspaceFiles.test.ts test/main/workspaceIpc.test.ts --test-name-pattern 'dangling AGENTS'` PASS；`npm run test:main` PASS，96 个 main tests 全部通过。
- Review RED 16：Codex 复审指出 finalize marker 后 draft source directory symlink swap 会把 workspace 外文件复制为 durable memory truth。新增 `finalize rejects draft directory symlink swap before copying source files`，targeted test 按预期失败，失败点为 finalize 返回 ok 且 durable audio 来自 workspace 外。
- Review GREEN 16：copy 前重新复核 draft source directory 为普通目录，并只复制普通 file entry；symlink source 触发 rollback 并保留 draft。`npx tsx --test test/main/memoryFiles.test.ts --test-name-pattern 'finalize rejects draft directory symlink swap before copying source files'` PASS；`npm run test:main` PASS，97 个 main tests 全部通过。
- Review RED 17：Subagent 复审指出 `.reo/drafts/recordings` ancestor symlink swap 仍会让 leaf-only `lstat` 跟随到 workspace 外 source。新增 `finalize rejects draft recordings ancestor symlink swap before copying source files`，targeted test 按预期失败，失败点为 finalize 返回 ok 且 durable audio 来自 workspace 外。
- Review GREEN 17：marker 后重新调用 `draftRecordingDirectory()` 走完整 workspace child containment，再复制普通 draft files。`npx tsx --test test/main/memoryFiles.test.ts --test-name-pattern 'finalize rejects draft recordings ancestor symlink swap before copying source files'` PASS；`npm run test:main` PASS，98 个 main tests 全部通过。
- Review RED 18：Codex 复审指出 `beforeExpose` 后 `memories/<memoryId>/recordings` parent symlink swap 会让 cached staging/target path 暴露 workspace 外 durable truth。新增 `finalize rejects recordings parent symlink swap before exposing staging`，targeted test 按预期失败，失败点为 finalize 返回 ok 且 outside target audio 被创建。
- Review GREEN 18：expose 前重新调用 `memoryRecordingDirectory()` 走完整 containment，复核 current staging directory，rollback 使用 safe workspace cleanup。`npx tsx --test test/main/memoryFiles.test.ts --test-name-pattern 'finalize rejects recordings parent symlink swap before exposing staging'` PASS；`npm run test:main` PASS，99 个 main tests 全部通过。
- Review RED 19：Codex 复审继续指出 draft copy、final rename、cleanup remove 和 workspace lock 写入仍有 post-validation symlink swap TOCTOU。新增 `finalize rejects draft recordings ancestor symlink swap after draft validation before copy`、`finalize rejects recordings parent symlink swap after rename validation`、`pre-expose cleanup does not delete outside staging after cleanup validation`、`workspace lock rejects .reo symlink swap before lock file write`。其中 draft copy、final rename、lock target 测试按预期失败；cleanup 场景在当前 Node 行为下未复现外部删除，但仍作为回归保护保留。
- Review GREEN 19：draft copy 在 hook 后再次走完整 draft containment；final rename 在 hook 后再次走 memory recording containment；safe cleanup 在 remove 前后各做一次 workspace cleanup resolution；workspace lock 在写 lock file 前复核 `.reo` directory identity。`npx tsx --test --test-name-pattern 'after draft validation before copy|after rename validation|after cleanup validation|symlink swap before lock file write|same memory id after marker write failure' test/main/memoryFiles.test.ts test/main/workspaceLock.test.ts` PASS；`npm run test:main` PASS，103 个 main tests 全部通过。
- Review RED 20：Codex 复审指出 finalize 成功路径 draft cleanup 仍可能在 validation 后跟随 `.reo/drafts/recordings` symlink 删除 workspace 外目录，recovery cleanup 仍可能在 validation 后跟随 `memories/<memoryId>/recordings` symlink 删除 workspace 外 staging；subagent 复审指出 final rename 后到 `memory.json` 写入之间可把 `memories/<memoryId>` 换成 symlink 并写出 workspace。新增 `finalize does not delete outside draft after draft cleanup validation`、`finalize rejects memory directory symlink swap before memory metadata write`、`recovery does not delete outside staging after recordings cleanup validation`，targeted tests 按预期失败 3 项。
- Review GREEN 20：draft cleanup 改用 safe cleanup helper 并在 unsafe 时失败回滚；memory metadata 写入前重新解析 `memoryDirectory()`；previous memory rollback 不再复用 cached memory path；recovery staging/invalid recording/draft cleanup 改用 safe cleanup helper；safe cleanup helper 同时接受 input root 和 canonical root 路径并用 canonical root 逐段 `lstat`。`npx tsx --test --test-name-pattern 'recovers an interrupted recording finalization|outside draft after draft cleanup validation|memory directory symlink swap before memory metadata write|outside staging after recordings cleanup validation' test/main/memoryFiles.test.ts` PASS；`npm run test:main` PASS，106 个 main tests 全部通过。
- Review RED 21：Codex 复审指出 recovery repair 写 `memory.json` 前仍复用旧 `memoriesDirectory`，memory directory swap 后可写出 workspace；同时 finalize 已删除 draft 但 marker 尚未清除的正常崩溃窗口会让 recovery 永久保留 marker。新增 `recovery clears marker when the stale draft is already missing` 和 `recovery does not write memory repair outside after memory directory swap`，`npm run test:main -- --test-name-pattern 'recovery clears marker when the stale draft is already missing|recovery does not write memory repair outside after memory directory swap'` 按预期失败 2 项。
- Review GREEN 21：recovery repair 写入前重新解析 `memoryDirectory()`；valid finalized recording 的 draft 已安全缺失时视作 draft cleanup 已完成，继续 fsync draft parent 并清 marker。targeted main tests PASS；`npm run verify:quick` PASS，main 108、renderer 34、typecheck、lint、format 全部通过。
- Review RED 22：Subagent 复审指出 finalize/recovery marker cleanup 仍使用 cached target path，`discardRecordingDraft` cleanup 仍可在 validation 后跟随 symlink parent 删除 workspace 外 draft，lock target 写入异常会直接 throw，handle close 在 release 失败时先删除 handle。新增 `finalize does not unlink outside marker after draft cleanup validation`、`recovery does not unlink outside marker after draft cleanup validation`、`discard draft does not delete outside draft after cleanup validation`、`workspace lock returns an error envelope when lock target cannot be written`、`initializeWorkspace returns an error envelope when the lock file target cannot be written`、`workspace handle remains registered when lock release fails`。Targeted main run 按预期失败 6 项。
- Review GREEN 22：marker unlink 前重新解析 `memoryRecordingDirectory()` 并复核 target；draft discard 复用 safe cleanup helper；lock target write failure 返回 `ERR_WORKSPACE_LOCK_FAILED`；handle close 只有 release 成功后删除 handle，release 失败保留 handle。Targeted main tests PASS；`npm run verify:quick` PASS，main 114、renderer 34、typecheck、lint、format 全部通过。
- Review RED 23：Codex 复审指出 audio chunk read 仍为每次 chunk 分配完整 audio file buffer，且 memory directory 创建阶段缺少 ancestor symlink swap 复核。新增 `audio chunk reads only the requested byte range`、`finalize rejects memories root symlink swap before creating a memory directory`、`finalize rejects memory directory symlink swap before creating recordings directory`。Targeted main run 按预期失败，失败点为 audio read hook 缺少 request length 上下文、memory directory 创建 hook 尚未存在。
- Review GREEN 23：audio chunk read 改为 no-follow file handle 上的 bounded `read(offset, length)`；memory 与 recordings directory 创建前后重新解析 workspace child containment。Targeted main tests PASS；`npm run verify:quick` PASS，main 117、renderer 34、typecheck、lint、format 全部通过。
- Review RED 24：Subagent/Codex 复审指出 finalized-only playback、append metadata rollback、AGENTS no-replace atomic、missing draft cleanup 和 recovery empty memory retry 五项缺口。新增 `audio playback reads finalized truth only, not draft audio`、`recording draft rolls back audio when metadata write fails after append`、`no-replace atomic workspace writes link the target and fsync the parent`、`finalize keeps durable recording when the draft is already missing at cleanup`、`recovery removes empty new-memory directories after staging cleanup`。Targeted run 先因缺少 no-replace helper TS2724 失败，随后各行为在旧实现下可复现失败。
- Review GREEN 24：playback manifest/chunk 改为 finalized-only；append metadata 写失败时 truncate audio 回 append 前 size 并 fsync；`AGENTS.md` 使用 no-replace atomic writer；safe missing draft cleanup 视为完成；recovery 清理 metadata-less empty new-memory directory 后允许 retry。`npm run typecheck` PASS；`npm run test:main` PASS，main 122；`npm run verify:quick` PASS，main 122、renderer 34、typecheck、lint、format 全部通过。
- REFACTOR 2：按 `/ycksimplify` 采纳低复杂度简化：删除 hidden memory id fallback，显式 `createMemoryId` owner；`workspaceIndexSchema` 复用 `workspaceMemorySummarySchema`；audio Blob 不再复制 chunks；transcript/reflections presence 改为 file size check；recording lookup 增加 status union，append 不再靠 error message 分支。`npm run typecheck` PASS；`npm run test:main -- --test-name-pattern "recording draft rolls back audio|audio playback reads finalized truth|find duplicate|stale draft|no-replace atomic|draft is already missing|empty new-memory"` PASS，main 122。
- Review RED 25：Subagent 复审指出 atomic workspace write 和 recording draft create/append 仍有 post-validation string-path write 窗口。新增 `atomic workspace writes keep final rename inside the validated parent directory`、`no-replace atomic workspace writes keep final link inside the validated parent directory`、`recording draft create rejects ancestor swap before writing draft files`、`recording append rejects ancestor swap before opening draft audio`。Subagent 已用临时目录复现旧实现会写 workspace 外；新增测试锁定该行为。
- Review GREEN 25：atomic writer 增加 parent directory identity 复核；draft create/append 增加 recordings root、draft directory 和 opened audio file identity 复核。`npm run typecheck` PASS；`npm run test:main -- --test-name-pattern "atomic workspace writes keep final rename|no-replace atomic workspace writes keep final link|recording draft create rejects ancestor swap|recording append rejects ancestor swap"` PASS，main 126。
- Review RED 26：Subagent 再次复审指出 atomic writer 最终 `rename/link`、draft metadata atomic commit 和 finalize expose 最终 `rename` 仍存在最终 path operation 窗口。新增 `finalize does not expose outside target after final rename validation`，并把 atomic tests 改为验证 final rename/link parent swap。
- Review GREEN 26：production atomic writer 改成在已验证 parent directory 内短 critical section 相对 commit；finalize expose 同样改为 validated parent directory 内相对 rename；parent path identity 改变时失败，不把 final target 写入 swapped outside parent。`npm run typecheck` PASS；`npm run test:main -- --test-name-pattern "atomic workspace writes keep final rename|no-replace atomic workspace writes keep final link|finalize does not expose outside target after final rename validation|recording draft create rejects ancestor swap|recording append rejects ancestor swap"` PASS，main 127。
- Review RED 27：最终复审继续指出 final commit 失败后 target cleanup、draft create 空目录、audio read ancestor swap 和 open 缺失托管目录仍需回归。新增或收紧 `atomic workspace writes keep final rename inside the validated parent directory`、`no-replace atomic workspace writes keep final link inside the validated parent directory`、`finalize does not expose outside target after final rename validation`、`recording draft create rejects ancestor swap before writing draft files`、`audio chunk reads reject finalized ancestor swap before opening audio`、`open workspace recreates missing managed directories before returning ready`。Targeted run 按预期失败 5 项；open managed directory RED 单独失败 1 项。
- Review GREEN 27：atomic writer/finalize expose 在后置 parent identity 失败时删除自己暴露的 target；draft create 清理自己刚创建的空 draft directory；audio read 在 no-follow open 前后绑定 finalized recording directory identity；open workspace 返回 ready 前创建缺失的 `.reo/drafts/recordings` 和 `memories/`；active recording draft 在当前进程内跳过每个 append 的 durable lookup，stale/manual draft 仍走 finalized lookup。`npm run test:main -- --test-name-pattern "open workspace recreates missing managed directories|atomic workspace writes keep final rename|audio chunk reads reject finalized ancestor swap|recording draft create rejects ancestor swap"` PASS，main 129。
- Review RED 28：Codex CLI 最终复审新增 4 个 P2：workspace lock release 失败后 lock 仍被视为 held、atomic replace 后置失败会删除唯一 target、不可读 `memories/` 被协调为空 index、recovery 会跟随 symlinked recording leaf directory。新增 `workspace lock is marked lost when release fails`、`atomic replace restores existing target after final rename validation fails`、`open workspace fails without replacing index when memories cannot be read`、`recovery drops recording references whose leaf directory is a symlink`。`npm run test:main` 按预期失败 4 项。
- Review GREEN 28：lock release 使用 `finally` 标记 lost；atomic replace 在同目录保留 backup，后置 validation/fsync 失败时恢复旧 target；rebuild/open 对存在但不可读的 directory 抛错并返回 `ERR_WORKSPACE_OPEN_FAILED`，不写空 index；recovery valid finalized check 先 `lstat` recording leaf directory 并拒绝 symlink。`npm run test:main` PASS，main 133。
- Review RED 29：使用额度恢复后的 subagent safety/correctness 复审新增 2 个 BLOCKER 和 2 个 MAJOR：symlinked `.reo/workspace.lock` leaf 会让 lock target 写到 workspace 外；recording finalize 在 `recordings/` parent validation 后创建 staging 时仍可能跟随 parent symlink swap；`memory.json` 和 finalized `recording.json` leaf symlink 会被 rebuild/detail 读取为 workspace 外 metadata；atomic replace 在 backup 已删除后发生 cleanup fsync failure 会误报失败。新增 `workspace lock rejects symlinked lock file leaf without writing outside`、`finalize rejects recordings parent symlink swap before creating staging`、`rebuild index rejects symlinked memory metadata leaf files`、`rebuild index rejects symlinked recording metadata leaf files`、`atomic replace does not report failure after successful commit and backup removal`。`npm run test:main` 按预期失败 5 项。
- Review GREEN 29：lock target 改为 `O_NOFOLLOW` 打开并拒绝 symlink leaf；finalize staging 在已验证 parent 内用相对 `mkdir` 创建并复核 parent identity；`memory.json` 与 finalized `recording.json` 读取改为 no-follow file handle；atomic replace 在 target commit 和 backup removal 已完成后把 cleanup durability 当作 best-effort，不再回滚或误报。`npm run test:main` PASS，main 138。
- REFACTOR：执行 `/simplify` 风格审查，修复 lint 发现的空 interface 和无效赋值；保留现有 helper 复用，未引入 generic repository/service/runtime；将 markdown save 的 durable write 与 best-effort projection refresh 拆开；复用窄的 workspace path helper 保护 `.reo` metadata directory，避免在 lock、init 和 draft 初始化中重复 unsafe path 判断。
- Review RED 30：后续 subagent `/simplify` 与 safety 复审指出 lock parent swap、staging 创建后 parent swap、unsafe finalized metadata no-fallback、markdown save parent swap 仍有阻断风险。新增 `workspace lock rejects .reo parent swap before lock target open`、`finalize rejects recordings parent symlink swap after creating staging`、`recording detail rejects unsafe finalized metadata instead of falling back to stale draft`、`saving markdown rejects recording parent swap before write`。`npm run test:main -- --test-name-pattern "lock target open|after creating staging|unsafe finalized metadata|parent swap before write"` 在旧实现下按预期失败 4 项。
- Review GREEN 30：lock 在当前 `.reo` directory identity 内打开 no-follow `workspace.lock` 并创建同目录 lock directory；finalize staging 创建后重新解析 staging 再写 marker、fsync、copy 和 expose；finalized lookup 增加 invalid durable status，detail/save 对 unsafe durable truth 不 fallback；detail 读取 metadata 使用 no-follow file handle；markdown save 在当前 recording directory identity 内写入；移除不再使用的 `proper-lockfile` 依赖。`npm run test:main -- --test-name-pattern "lock target open|after creating staging|unsafe finalized metadata|parent swap before write"` PASS，main 142；`npm run test:main` PASS，main 142。
- Review RED 31：safety subagent 最终复审指出 window close/renderer crash 会泄漏 workspace lock、托管 `.reo/drafts` 子目录创建仍有 ancestor symlink TOCTOU、final expose 不是 no-replace、finalize 读取 staging `recording.json` 未显式 no-follow。新增 `main window teardown releases workspace handles`、`workspace handle store releases all handles for window teardown`、`managed draft directory creation rejects .reo ancestor swap before child mkdir`、`finalize rejects recording target created after duplicate preflight`、`finalize rejects staging metadata symlink after draft copy`。Targeted run 在旧实现下按预期失败 4 项，staging metadata symlink 已由既有 fsync 路径挡住并保留为回归。
- Review GREEN 31：BrowserWindow `closed` 和 `render-process-gone` 调用 close-all handles；handle store 支持 teardown close-all；托管 child directory 在已验证 parent identity 内相对创建并复核 parent；final expose 在已验证 parent 内 no-replace 相对 rename；staging metadata 通过 no-follow file helper 读取。`npm run test:main -- --test-name-pattern "main window teardown|managed draft directory|late duplicate|staging metadata symlink|releases all handles"` PASS，main 147；`npm run verify:quick` PASS，main 147、renderer 34、typecheck、lint、format 全部通过。
- Review RED 32：safety/simplify subagent 复审指出 root identity 未绑定、teardown release failure 丢 handle、workspace metadata/index follow symlink、audio read 未绑定 finalized metadata length、markdown presence follow symlink，以及 open 双扫描。新增 `workspace lock is unusable when workspace root identity changes`、`workspace handle store keeps handles whose teardown release fails`、`open workspace rejects symlinked workspace metadata`、`open workspace rebuilds instead of trusting a symlinked index`、`audio reads reject bytes beyond finalized metadata length after audio grows`、`index rebuild ignores symlinked transcript and reflections presence files`。RED 首次运行先因 `WorkspaceLock.isUsable`/`WorkspaceHandleLock.isUsable` 缺失出现 TypeScript failure，补接口后旧实现按行为失败。
- Review GREEN 32：lock 绑定 root/`.reo` identity 并用 `isUsable()` 区分“artifact 可能仍在”与“可授权写入”；handle require 检查 lock usability；teardown close-all 只删除 release 成功的 handle；workspace metadata/index 使用 no-follow file handle；audio manifest/chunk 用 finalized metadata `audioByteLength` 约束最终 file handle；markdown presence 改用 `lstat`；open workspace 一次 read model 同时产出 `memories[]` 和临时 `recordings[]`。`npm run test:main -- --test-name-pattern "root identity changes|teardown release fails|symlinked workspace metadata|symlinked index|symlinked transcript|metadata length after audio grows|marked lost when release fails"` PASS，main 153；`npm run verify:quick` PASS，main 153、renderer 34、typecheck、lint、format 全部通过。
- Review RED 33：safety/correctness 与 `/simplify` 复审指出 safe resolution 后绝对 `mkdir`、final expose check-then-rename、read model root swap、post-lock/pre-file-write lock loss、delayed handle usability、audio chunk 每次全 workspace lookup、markdown autosave 每次全 workspace rebuild 仍需收口。新增 `initializeWorkspace rejects lock identity loss before workspace files are written`、`openWorkspace rejects lock identity loss before workspace files are opened`、`finalize rejects memories root swap after resolving the memory mkdir target`、`finalize rejects memory directory swap after resolving the recordings mkdir target`、`finalize rejects recording target created after final duplicate preflight`、`rebuild preserves the existing index when memories root changes before scan`、`audio chunk reads reuse the finalized audio target after the first lookup`、`saving markdown refreshes one memory index entry without a full workspace rescan`、`required workspace handles can recheck lock usability before delayed filesystem work`。`npm run test:main` RED 失败为缺少对应 hook/API 和 behavior，证明 Review 33 缺口可复现。
- Review GREEN 33：memory/recordings directory 创建改为在已验证 parent identity 内相对 `mkdir`，final expose 改为相对 `mkdir` no-replace 保留 target 后搬迁 staging 内容；read model 绑定 `memories/` root identity，identity 变化时保留既有 index；initialize/open lock 后写文件前复核 `isUsable()`；required handle 暴露 delayed `assertUsable()`；audio playback read target 增加 main process 内 cache，但最终读取仍用 no-follow file handle 和 finalized metadata length 复核；markdown save 改为刷新所属 memory index entry。`npm run test:main` PASS，main 162。
- Review RED 34：subagent safety/correctness 复审指出 finalize copy 后 staging draft metadata 只做 `JSON.parse`，无 schema 校验；恶意或并发篡改 `nextSequence` 等字段时，会暴露 invalid durable recording truth、删除 draft，并返回 `draft-preserved`。新增 `finalize rejects invalid draft metadata before exposing durable recording truth`。`npm run test:main -- --test-name-pattern "invalid draft metadata"` 按预期失败，失败点为 draft 已被删除。
- Review GREEN 34：finalize 在 durable metadata 写入前按 draft metadata schema 解析 staging `recording.json`，并校验 `workspaceId`、`recordingId` 和 `audioByteLength` 与请求及实际 audio file 一致。`npm run test:main -- --test-name-pattern "invalid draft metadata"` PASS，main 168。
- Review RED 35：subagent safety/correctness 复审指出 audio finalized target cache 命中后不再检查其他 memory 下是否新出现同 `recordingId` 的 duplicate finalized directory。新增 `cached audio reads reject duplicate finalized ids created after cache fill`。`npm run test:main -- --test-name-pattern "duplicate finalized ids created after cache"` 按预期失败，失败点为 manifest 仍返回 ok。
- Review GREEN 35：cache 命中后先执行窄 duplicate finalized directory guard，再复核 cached metadata 和 no-follow audio file handle。`npm run test:main -- --test-name-pattern "duplicate finalized ids created after cache"` PASS，main 169；`npm run test:main` PASS，main 169。
- Review RED 36：subagent safety/correctness 复审指出 recording draft leaf directory 创建后 ancestor swap 可写到 workspace 外；`workspaceIndexWriteQueues` 在队列外预先读取 memory summary 会让并发 markdown save 用 stale summary 覆盖新 projection。新增 `recording draft create rejects ancestor swap after leaf directory create` 和 `markdown index refresh reads memory summary inside the workspace index queue`。Targeted run 在旧实现下按预期失败。
- Review GREEN 36：draft create 在 leaf mkdir 后绑定 leaf identity，写 audio/metadata 前重新解析完整 draft path，cleanup 只删除 identity 未变的 leaf；markdown index refresh 在 workspace index write queue 内重新读取当前 memory summary。`npm run test:main -- --test-name-pattern "after leaf directory create|summary inside the workspace index queue"` PASS。
- REFACTOR 3：按 `/simplify` 和效率复审修复 production API test hook 泄漏、directory identity 重复和 IPC handler 重复；atomic writer 改为异步 temp write/fsync + 短同步 final commit；audio chunk cache 命中不再按 chunk 重扫 duplicate ownership。`npx tsc -p tsconfig.main.json --noEmit` PASS；`npm run test:main -- --test-name-pattern "recording finalize preserves draft transcript|saving transcript and reflections|saving markdown refreshes one memory|concurrent markdown saves|markdown index refresh|saving markdown reports index|atomic workspace writes|no-replace atomic workspace writes"` PASS，main 171；`npm run test:main -- --test-name-pattern "audio chunk reads reuse|cached audio chunk reads do not rescan|cached audio reads reject duplicate|audio manifest and chunk reads"` PASS，main 172。
- Review RED 38：最终复审指出 cache 命中后 chunk read 不重验 finalized metadata、final expose 仍可能跟随被替换的 staging source、recovery 会删除 marker-bearing durable recording、uncaught exception 会泄漏 lock、durable transaction 内部缺少 delayed handle usability assertion、append metadata 写失败错误码不准确、stale lock directory 会永久阻断 open。新增 `cached audio chunk reads reject stale finalized metadata after cache fill`、`finalize rejects staging source swap before final expose`、`recovery preserves marker-bearing durable recording when finalized files are invalid`、`main window teardown releases workspace handles`、`finalize aborts when workspace handle is lost during durable transaction`、`recording draft rolls back audio when metadata write fails after append`、`workspace lock replaces stale lock directories owned by dead processes`。RED 首轮按预期失败 4 项，后续新增 compile RED 锁定 `assertWorkspaceUsable` 入口，append/lock 行为 RED 均按预期失败。
- Review GREEN 38：cached chunk path 每次重验 cached metadata；final expose 绑定 source staging identity；recovery 对 memory metadata 已引用但当前 finalized validation 失败的 marker-bearing durable recording fail-open；uncaught exception 先 close-all handles 再 exit；memory finalize/copy/expose/cleanup 阶段消费 delayed workspace usability assertion；append audio chunk 写入后先 fsync，再写 metadata，metadata 失败时 truncate/fsync 并返回 `ERR_RECORDING_APPEND_FAILED` + `draft-preserved`；workspace lock 写 owner pid 并替换死进程 stale lock；metadata/audio/markdown read/write 继续收口到已验证 directory identity 下。`npm run test:main` PASS，main 180；`npm run verify:quick` PASS，main 180、renderer 34、typecheck、lint、format 全部通过。
- Review RED 39：Codex CLI 最终 `$ycksimplify` 复审新增 BLOCKER/MAJOR：lock directory identity 未绑定、marker-bearing invalid finalized fail-open 会清空 memory reference、create/append/markdown delayed handle usability 不足、chunk IPC 可绕过 duplicate revalidation、`recording.json` schema owner 分叉、finalize copy 和 playback 串行性能风险。新增或增强 `workspace lock is unusable when its lock directory is replaced`、`recovery preserves marker-bearing durable recording when finalized files are invalid`、`recording draft create aborts when workspace handle is lost before draft files are written`、`recording append aborts when workspace handle is lost before audio write`、`recording markdown save aborts when workspace handle is lost before write`、`cached audio chunk reads revalidate duplicate ownership per chunk`、`cached audio chunk reads reject duplicate finalized ids without a fresh manifest`。RED 按预期失败；`npx tsc -b --pretty false` 在 schema refactor 中先暴露 renderer narrowing 问题并已修复。
- Review GREEN 39：recovery fail-open 保留 `memory.json.recordingIds`；lock 绑定 lock directory identity，旧 handle 不再删除新 owner lock；create/append/markdown save 在关键写入前重验 handle usability；chunk cache 命中每次重验 duplicate finalized id；`recordingMetadata.ts` 成为 `recording.json` schema owner；finalize draft copy 改为异步 fd read/write/fsync；renderer playback 最多并发 4 个 chunk read。`npm run test:main` PASS，main 185。
- Review RED 40：Claude CLI `/simplify` 复审新增 lock liveness/security MAJOR：partial stale lock directory 缺失 owner file 会永久阻断 open，owner file 读取未 no-follow。新增 `workspace lock replaces stale lock directories with missing owner files` 和 `workspace lock replaces stale lock directories with symlinked owner files`；RED 按预期失败。
- Review GREEN 40：lock owner 读取改为 no-follow file open + file 校验 + 小文件 JSON parse；missing/malformed/symlinked owner file 视为 invalid stale lock 并替换，symlink target 不被读取或删除。`npm run test:main -- --test-name-pattern "stale lock directories with missing owner|stale lock directories with symlinked owner"` PASS；`npm run test:main` PASS，main 187。
- Review RED 41：子代理复审新增 delayed handle usability BLOCKER 和 verification evidence MAJOR。新增 `recording draft create aborts when workspace handle is lost before draft directory create`、`finalize aborts when workspace handle is lost before memory parent writes`、`finalize aborts when workspace handle is lost before staging writes` 和 `limits playback chunk reads to four concurrent requests`。`npm run test:main -- --test-name-pattern "draft directory create|memory parent writes|staging writes"` 在旧实现下按预期失败 3 项，失败点均为 lock lost 后仍触达后续写入 hook。
- Review GREEN 41：draft create 在 directory create 前消费 delayed handle usability；finalize 在 parent/staging hook 后和 marker 写入前消费 delayed handle usability；renderer playback 增加最多 4 个并发 chunk read 的行为证据。`npm run test:main -- --test-name-pattern "draft directory create|memory parent writes|staging writes"` PASS，main 190；`npx vitest run src/renderer/src/workspace/RecordingOverlay.test.tsx` PASS，14 个 overlay tests 全部通过。
- Review RED 42：子代理复审新增 lock lost 后 rollback/index/partial recovery BLOCKER。新增 `finalize does not roll back files after the workspace handle is lost`、`finalize rechecks the workspace handle before rebuilding the index`、`recovery removes markerless partial new-memory recordings for retry` 和 `saving markdown rechecks the workspace handle before refreshing the index`。`npm run test:main -- --test-name-pattern "does not roll back|rechecks the workspace handle before rebuilding|markerless partial|workspace handle before refreshing"` 在旧实现下按预期失败 4 项。
- Review GREEN 42：`WorkspaceHandleLost` catch 立即停止旧 handle cleanup/rollback；memory metadata 与 markdown durable write 后、index write 前重新消费 handle usability；final expose 改为已验证 parent 内原子 rename 并重验 target missing；metadata-less partial new-memory recovery 清理仅剩 `recordings/` 的目录以允许 retry。`npm run test:main -- --test-name-pattern "final rename validation|final duplicate preflight|does not roll back|rechecks the workspace handle before rebuilding|markerless partial|workspace handle before refreshing"` PASS，main 194。
- Review RED 43：子代理复审新增 pre-expose lock lost cleanup 与 metadata-less marker-bearing recovery BLOCKER。新增 `finalize does not run pre-expose cleanup after the workspace handle is lost` 和 `recovery preserves metadata-less marker-bearing finalized recordings`。`npm run test:main -- --test-name-pattern "pre-expose cleanup after the workspace handle is lost|metadata-less marker-bearing"` 在旧实现下按预期失败 2 项。
- Review GREEN 43：pre-expose catch 遇到 `WorkspaceHandleLost` 立即停止旧 handle cleanup；recovery 对缺失 `memory.json` 但带 marker 且含 `recording.json` 或 `audio.webm` 的 finalized recording payload fail-open 保留，marker-only partial 仍允许 retry cleanup。`npm run test:main -- --test-name-pattern "interrupted recording finalization|pre-expose cleanup after the workspace handle is lost|metadata-less marker-bearing|markerless partial"` PASS，main 196。
- Review RED 44：子代理复审新增 read model persist identity 与 close panel Blob URL revoke MAJOR。新增 `rebuild preserves the existing index when memories root changes before persist` 和 `revokes playback Blob URL when closing the recording panel`。`npm run test:main -- --test-name-pattern "memories root changes before persist"` 在旧实现下按预期失败 1 项；`npx vitest run src/renderer/src/workspace/RecordingOverlay.test.tsx --testNamePattern "revokes playback Blob URL when closing"` 在旧实现下按预期失败 1 项。
- Review GREEN 44：read model persist `.reo/index.json` 前重验 scan 绑定的 `memories/` root identity；close recording panel 清空 `playbackUrl` 并触发既有 cleanup revoke。`npm run test:main -- --test-name-pattern "memories root changes before persist"` PASS，main 197；`npx vitest run src/renderer/src/workspace/RecordingOverlay.test.tsx --testNamePattern "revokes playback Blob URL when closing"` PASS。
- Review RED 45：Codex CLI `$ycksimplify` 最终复审新增 2 个 BLOCKER 和 3 个 MAJOR。新增 `open workspace preserves the existing index when memories root changes before reconciliation persist`、`finalize rejects recording target created after the last target preflight` 和 `does not create a Blob URL when playback finishes after closing the recording panel`，RED 首轮分别按预期失败；补充 `full index rebuild cannot overwrite a queued markdown index refresh` 与 `open workspace rejects workspace metadata when .reo changes during read` 锁定全量 index writer queue 和 metadata parent identity。
- Review GREEN 45：open reconciliation persist 进入同一 index replace path 并在写前重验 `memories/` identity；final expose 改为 no-replace target reservation + marker-first staging move；close 后 playback session 失效，过期 playback request 不创建 Blob；full index replace 和 single entry refresh 共用 index write queue；metadata/index no-follow read 后复核 parent identity。`npx tsc -p tsconfig.main.test.json && node --test --test-name-pattern "full index rebuild cannot overwrite|metadata when .reo changes|reconciliation persist|last target preflight" ...` PASS；`npm run test:main` PASS，main 201；`npm run test:renderer` PASS，renderer 37。
- Review RED 46：子代理最终复审新增 1 个 BLOCKER 和 1 个 MAJOR。新增 `openWorkspace rejects lock identity loss during index reconciliation` 和 `full index rebuild computes replacement after queued markdown refresh`。RED 首轮先因缺少 `setBeforeReadModelReplaceForTest` 出现 TypeScript failure，随后旧实现可复现 open 中途 lock loss 后继续写未持锁 `.reo`、full rebuild 用 stale payload 覆盖 markdown refresh。
- Review GREEN 46：open 文件阶段在托管目录补齐、recovery、read model rebuild、index reconciliation 和返回 session 前消费 lock usability；full index replace 在 workspace index write queue 内重新读取当前 read model 后写入。`npx tsc -p tsconfig.main.test.json && node --test --test-name-pattern "lock identity loss during index reconciliation|computes replacement after queued markdown refresh" ...` PASS；扩展定向 run 覆盖 `full index rebuild cannot overwrite`、`open workspace preserves the existing index when memories root changes before reconciliation persist` 和 `openWorkspace rejects lock identity loss before workspace files are opened`，5 项 PASS。
- Review RED 47：Codex CLI 和子代理复审指出 open 托管目录创建时 lock loss 仍可写入新 `.reo`、open reconciliation 仍可用队列外 stale payload 覆盖 metadata refresh、recovery cleanup 前未消费 lock usability、unmount 后 pending playback 仍可创建 Blob URL。新增 `open workspace does not create drafts when lock identity is lost during drafts ensure`、`open workspace reconciliation computes replacement after a metadata refresh`、`recovery stops before stale staging cleanup when workspace usability is lost`、`does not create a Blob URL when playback finishes after unmount`。`npx tsc -p tsconfig.main.test.json` 首轮按预期失败 TS2353，renderer targeted test 按预期失败，错误为 `createObjectURL` 被调用 1 次。
- Review GREEN 47：托管目录创建在相对 parent mkdir 前消费 lock usability，early abort 返回原始 lock lost envelope；open reconciliation replace 在 workspace index queue 内重新读取当前 read model；recovery 写操作前消费 usability；recording overlay unmount 失效 playback session。`npx tsc -p tsconfig.main.test.json` PASS；`node --test --test-name-pattern "open workspace does not create drafts|open workspace reconciliation computes replacement|recovery stops before stale staging cleanup" ...` PASS，3 项通过；`npx vitest run src/renderer/src/workspace/RecordingOverlay.test.tsx --testNamePattern "does not create a Blob URL when playback finishes after unmount"` PASS。
- Review RED 48：Codex CLI `$ycksimplify` 复审指出 initialize 中途 lock loss 仍可能写 workspace files、open metadata revalidation 前 lock lost 会被 metadata/unsafe error 掩盖、metadata-less cleanup 删除前未重新消费 usability、finalize 删除 draft 后 fsync/marker 前未重新消费 delayed handle usability、active spec 与 current/code 在 audio chunk duplicate revalidation 上不一致。新增 `initializeWorkspace rejects lock identity loss during managed directory creation`、`open workspace reports lock lost before target revalidation errors`、`recovery stops before metadata-less memory cleanup when workspace usability is lost` 和 `finalize keeps durable marker when workspace lock is lost after draft cleanup`。`node --test --test-name-pattern ...` 在旧实现下按预期失败 4 项。
- Review GREEN 48：initialize 文件阶段传入并消费 lock usability；open 从 target revalidation 前开始消费 usability，metadata invalid 前先确认不是 lock lost；metadata-less cleanup helper 在实际 remove 前再次消费 usability；finalize 删除 draft 后在 draft parent fsync 和 marker unlink 前重新消费 delayed handle usability；active spec 与 current/code 对齐为 chunk read 继续重验 duplicate finalized id。`npx tsc -p tsconfig.main.test.json` PASS；`node --test --test-name-pattern "initializeWorkspace rejects lock identity loss during managed directory creation|open workspace reports lock lost before target revalidation errors|recovery stops before metadata-less memory cleanup|finalize keeps durable marker when workspace lock is lost after draft cleanup" ...` PASS，4 项通过。
- Review RED 49：Task 1 最后一轮子代理与 Codex CLI 复审指出 atomic write 内 lock loss、audio read delayed usability、symlink marker fail-open、lock owner 写入窗口、cold invalid duplicate、index replace write 前 identity、same-file markdown save 顺序、activeDrafts root state、discard delayed usability、close 后 playback chunk drain、unknown draft file promotion、metadata/index JSON 无上限。新增对应 13 条 RED/回归测试；旧实现按预期失败或先以缺失 test seam 的 TypeScript failure 暴露。
- Review GREEN 49：atomic writer 消费 usability；audio manifest/chunk read 在 open/read 窗口重验 handle；recovery marker no-follow；lock owner 在已绑定 lock directory identity 内 no-follow 写入并 fsync；lookup 遇到同 id invalid durable truth 返回 invalid-durable；index replace 在 queue 内 read 后写前复核；markdown save 增加 same-file queue；close/teardown 清 root-scoped recording runtime state；discard remove 前消费 usability；playback stale 后停止调度 chunk；finalize 只复制 allowlist draft files；metadata/index 增加 1 MiB 上限并收紧 strict schema。`npx tsc -p tsconfig.main.test.json && node --test --test-name-pattern "owner write|invalid duplicate|oversized finalized metadata|after replacement read|unknown draft files|latest same-file edit|discard draft aborts|root draft state is cleared|workspace handle is lost before audio open|symlinked finalize markers|AGENTS when lock is lost" ...` PASS，12 项通过；`npx vitest run src/renderer/src/workspace/RecordingOverlay.test.tsx --testNamePattern "stops scheduling playback"` PASS。

## 定向测试

已运行：

```bash
npx tsx --test test/main/workspaceIpc.test.ts
npx tsx --test test/main/recordingDrafts.test.ts
npx tsx --test test/main/memoryFiles.test.ts
npm run test:main -- --test-name-pattern "only the requested byte range|memories root symlink swap before creating a memory directory|memory directory symlink swap before creating recordings directory"
npm run test:main -- --test-name-pattern "recording draft rolls back audio|audio playback reads finalized truth|find duplicate|stale draft|no-replace atomic|draft is already missing|empty new-memory"
npm run test:main -- --test-name-pattern "atomic workspace writes keep final rename|no-replace atomic workspace writes keep final link|finalize does not expose outside target after final rename validation|recording draft create rejects ancestor swap|recording append rejects ancestor swap"
npm run test:main -- --test-name-pattern "open workspace recreates missing managed directories|atomic workspace writes keep final rename|audio chunk reads reject finalized ancestor swap|recording draft create rejects ancestor swap"
npm run test:main -- --test-name-pattern "lock target open|after creating staging|unsafe finalized metadata|parent swap before write"
npm run test:main -- --test-name-pattern "main window teardown|managed draft directory|late duplicate|staging metadata symlink|releases all handles"
npm run test:main -- --test-name-pattern "root identity changes|teardown release fails|symlinked workspace metadata|symlinked index|symlinked transcript|metadata length after audio grows|marked lost when release fails"
npm run test:main -- --test-name-pattern "invalid draft metadata"
npm run test:main -- --test-name-pattern "duplicate finalized ids created after cache"
npm run test:main -- --test-name-pattern "after leaf directory create|summary inside the workspace index queue"
npm run test:main -- --test-name-pattern "recording finalize preserves draft transcript|saving transcript and reflections|saving markdown refreshes one memory|concurrent markdown saves|markdown index refresh|saving markdown reports index|atomic workspace writes|no-replace atomic workspace writes"
npm run test:main -- --test-name-pattern "audio chunk reads reuse|cached audio chunk reads revalidate|cached audio reads reject duplicate|audio manifest and chunk reads"
npm run test:main -- --test-name-pattern "lock_lost|handle is lost before"
npm run test:main -- --test-name-pattern "stale lock directories with missing owner|stale lock directories with symlinked owner"
npm run test:main -- --test-name-pattern "draft directory create|memory parent writes|staging writes"
npm run test:main -- --test-name-pattern "does not roll back|rechecks the workspace handle before rebuilding|markerless partial|workspace handle before refreshing"
npm run test:main -- --test-name-pattern "final rename validation|final duplicate preflight|does not roll back|rechecks the workspace handle before rebuilding|markerless partial|workspace handle before refreshing"
npm run test:main -- --test-name-pattern "interrupted recording finalization|pre-expose cleanup after the workspace handle is lost|metadata-less marker-bearing|markerless partial"
npm run test:main -- --test-name-pattern "memories root changes before persist"
npx tsc -p tsconfig.main.test.json && node --test --test-name-pattern "full index rebuild cannot overwrite|metadata when .reo changes|reconciliation persist|last target preflight" .tmp/test-main/test/main/recordingReads.test.js .tmp/test-main/test/main/workspaceFiles.test.js .tmp/test-main/test/main/memoryFiles.test.js
npx tsc -p tsconfig.main.test.json && node --test --test-name-pattern "lock identity loss during index reconciliation|computes replacement after queued markdown refresh" .tmp/test-main/test/main/workspaceIpc.test.js .tmp/test-main/test/main/recordingReads.test.js
node --test --test-name-pattern "full index rebuild cannot overwrite|full index rebuild computes replacement|openWorkspace rejects lock identity loss|open workspace preserves the existing index when memories root changes before reconciliation persist" .tmp/test-main/test/main/recordingReads.test.js .tmp/test-main/test/main/workspaceIpc.test.js .tmp/test-main/test/main/workspaceFiles.test.js
npx tsc -p tsconfig.main.test.json
node --test --test-name-pattern "open workspace does not create drafts|open workspace reconciliation computes replacement|recovery stops before stale staging cleanup" .tmp/test-main/test/main/workspaceFiles.test.js .tmp/test-main/test/main/memoryFiles.test.js
node --test --test-name-pattern "initializeWorkspace rejects lock identity loss during managed directory creation|open workspace reports lock lost before target revalidation errors|recovery stops before metadata-less memory cleanup|finalize keeps durable marker when workspace lock is lost after draft cleanup" .tmp/test-main/test/main/workspaceIpc.test.js .tmp/test-main/test/main/workspaceFiles.test.js .tmp/test-main/test/main/memoryFiles.test.js
npx tsc -p tsconfig.main.test.json && node --test --test-name-pattern "owner write|invalid duplicate|oversized finalized metadata|after replacement read|unknown draft files|latest same-file edit|discard draft aborts|root draft state is cleared|workspace handle is lost before audio open|symlinked finalize markers|AGENTS when lock is lost" .tmp/test-main/test/main/workspaceLock.test.js .tmp/test-main/test/main/recordingReads.test.js .tmp/test-main/test/main/memoryFiles.test.js .tmp/test-main/test/main/recordingDrafts.test.js .tmp/test-main/test/main/workspaceFiles.test.js
npx vitest run src/renderer/src/workspace/RecordingOverlay.test.tsx --testNamePattern "revokes playback Blob URL when closing"
npx vitest run src/renderer/src/workspace/RecordingOverlay.test.tsx --testNamePattern "playback finishes after closing|revokes playback Blob URL when closing|limits playback chunk reads"
npx vitest run src/renderer/src/workspace/RecordingOverlay.test.tsx --testNamePattern "does not create a Blob URL when playback finishes after unmount"
npx vitest run src/renderer/src/workspace/RecordingOverlay.test.tsx --testNamePattern "stops scheduling playback"
npx vitest run src/renderer/src/workspace/RecordingOverlay.test.tsx
npm run test:main
npm run test:renderer
npm run verify:quick
```

当前最新结果：Review 49 修复后 `npm run verify:quick` PASS：main 222、renderer 39、typecheck、lint、format 全部通过。

## 固定门禁

已运行：

```bash
npm run verify:quick
git diff --check
diff -u AGENTS.md .claude/CLAUDE.md
find docs/specs -mindepth 1 -maxdepth 1 -print
git status --short
```

当前结果：

- `npm run verify:quick` PASS：Review 49 后 main 222、renderer 39、typecheck、lint、format 全部通过。
- `git diff --check` PASS，空输出。
- `diff -u AGENTS.md .claude/CLAUDE.md` PASS，空输出。
- `find docs/specs -mindepth 1 -maxdepth 1 -print` PASS，当前只输出本 spec：`docs/specs/2026-05-06-1254-first-product-slice-task-1-workspace-file-truth`。
- `git status --short` PASS，当前只显示本 task owns 的文件。

归档前仍需再次运行：

- `find docs/specs -mindepth 1 -maxdepth 1 -print`
- `git status --short`

## 当前证据

- Task spec 已创建为当前唯一 active spec。
- Design-hardening spec 已归档，`docs/specs/*` 在创建本 task spec 前为空。
- 当前实现移除了不再使用的 `proper-lockfile` 依赖，没有新增依赖。
- 当前实现触碰了 renderer 类型和 session projection merge，不触碰视觉 UI。
- Runtime/Computer Use 未运行；本 task 是 main-side filesystem transaction slice，不涉及 OS dialog、mic permission 或视觉 reference。
- `docs/current/electron.md` 仅修正 microphone intent 当前事实，不在本 task 实现 permission intent。
- Claude CLI 审查按用户要求只传 prompt、model `claude-opus-4-7` 和 effort `max`；Review 28 后只读 `/simplify` 复审结论 PASS，随后 subagent safety/correctness 复审发现 Review 29 阻断点并已修复。
- Review 47 修复后，`npm run verify:quick` PASS：main 206、renderer 38、typecheck、lint、format 全部通过。
- Review 49 修复后，定向 main/renderer tests PASS；`npm run verify:quick` PASS：main 222、renderer 39、typecheck、lint、format 全部通过。子代理与 Codex CLI 的最后一轮 BLOCKER/MAJOR 已修复；Claude CLI 上次受额度限制，提示 `You've hit your limit · resets 5:40am (America/Los_Angeles)`；用户已在 2026-05-07 05:06 PDT 明确豁免本轮 Claude CLI 复审，不再作为 Task 1 归档阻断。
- 2026-05-07 05:08 PDT 归档前固定门禁：
  - `npm run verify:quick` PASS：main 222、renderer 39、typecheck、lint、format 全部通过。
  - `git diff --check` PASS，空输出。
  - `diff -u AGENTS.md .claude/CLAUDE.md` PASS，空输出。
  - `find docs/specs -mindepth 1 -maxdepth 1 -print` PASS，只列出 `docs/specs/2026-05-06-1254-first-product-slice-task-1-workspace-file-truth`。
- 2026-05-07 05:10 PDT 归档后固定门禁：
  - `npm run verify:quick` PASS：main 222、renderer 39、typecheck、lint、format 全部通过。
  - `git diff --check` PASS，空输出。
  - `diff -u AGENTS.md .claude/CLAUDE.md` PASS，空输出。
  - `find docs/specs -mindepth 1 -maxdepth 1 -print` PASS，空输出。
