# Tasks

- [x] T1：复现并记录当前点击错位根因。
- [x] T2：写 RED 测试覆盖连续 read preview 行点击到 source line，以及复杂 preview block 点击后揭开 source。
- [x] T3：GREEN 修复点击定位与 active preview block source reveal，不使用固定偏移。
- [x] T4：重跑 focused tests 与运行时验证。
- [ ] T5：启动 `npm run dev`，等待用户真实鼠标复测。
- [x] T6：对 Note Markdown editor 当前截图执行独立盲审，审查提示不得暴露 Reo、CM6 或实现路线。
- [x] T7：移除 Note surface 的第二编辑器路径，确保 inline、overlay、runtime fixture 和 handoff 都走同一 NoteMarkdownEditor owner。
- [x] T8：修复编辑态 Setext heading 抢占上一行样式，确保输入 `-` 后上一行不被标题化。
- [x] T9：修复记忆空间 inline read-to-edit 点击 anchor 丢失，确保点击空行后输入仍落在被点击行。
- [x] T10：修复沉浸式 overlay 与背景 Memory Studio 同时挂载同一 Note CM6 editor，Segment 和 SegmentSupplement 都只保留 handoff 占位。
- [x] T11：修复沉浸式 Markdown 工具栏位置和编辑 pane 外层 padding，避免工具栏漂到右侧和正文出现多层容器感。
- [x] T12：修复 CM6 default markdown highlighter 在编辑态仍把 Setext heading 文本加粗/下划线的问题，确保 raw `=`/`-` 可见时上一行保持普通文本。
- [x] T13：修复复杂 preview block 存在时点击 `.cm-content`、行间空白或 block 旁侧空白定位错误的问题，read/edit 两种模式都先使用可见 DOM line/widget 几何反查 source anchor。
- [x] T14：修复编辑态 ArrowUp/ArrowDown 经过 inactive preview block 时跳过复杂区块 source 行和上下空行的问题，按 Markdown source line 逐行移动。
- [x] T15：修复 `verify:quick` 暴露的沉浸式工具栏打开回归，让 Reo host capture 层稳定通知 toolbar owner，不把 toolbar 可见性绑定到 CM6 内部 DOM 事件细节。
- [x] T16：修复 `verify:quick` 暴露的粘贴图片后预览缺失回归，让 inline image 在光标位于 source 末端时恢复预览，同时保留 source range 内编辑 raw Markdown 的行为。
- [x] T17：重跑完整 `npm run verify:quick` 并记录最终门禁结果。
- [x] T18：按 `/review` 和 `/ycksimplify` 要求执行独立盲审，判断是否仍存在新旧编辑器共存、重复状态模型、假控件或投机式实现。
- [x] T19：删除编辑器目录下重复 autosave machine，把保存 patch 派生收回共享 autosave 真源。
- [x] T20：修复 Markdown 预览链接和带链接图片的假控件，点击必须走 main-owned 安全外链 IPC。
- [x] T21：修复附件写入期间 workspace lock 丢失仍可落盘，以及 note finalize cleanup 按路径误删替换目录的问题。
- [ ] T22：继续收口独立盲审指出的 click/keyboard geometry 补丁和 preview widget 交互层设计问题。
